import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Color (Wah Industries Corporate Navy)
  static const Color primary = Color(0xFF0D233A);
  static const Color primaryLight = Color(0xFF1E3A5F);

  // Secondary / Accent Color (Industrial Gold)
  static const Color accent = Color(0xFFD4AF37);
  static const Color accentLight = Color(0xFFE8C86B);

  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF4F6F8); // Clean cool grey
  static const Color surface = Color(0xFFFFFFFF); // Pure white for cards/inputs
  static const Color darkBackground = Color(0xFF0A1624);

  // Text Colors
  static const Color textPrimary = Color(0xFF1E293B); // Deep slate charcoal
  static const Color textSecondary = Color(0xFF64748B); // Muted slate grey

  // Status Colors
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
}
